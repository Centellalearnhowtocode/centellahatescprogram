#include <stdio.h>
#include <math.h>
int main(){

  float x = 1/2;
  //x = sqrt(x);
  //x = pow(x,3);
  //x = round(x); make it less
  //x = floor(x); make it full number
  //x = ceil(x); //make it more
  //x = abs(x); //|x|
  x = cos(x);
  //x = log(x);
  printf("%f",x);
  return 0;
}