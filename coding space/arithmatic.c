#include <stdio.h>
#include <math.h>
int main(void){
  // % the remaining of the division of two numbers

  //++ increment operator by 1
  //-- decrement operator by 1
  int x = 10;
  int y = 2;
  int z = 0;

  x =x*2;
  printf("%d",x);

  return 0;
}